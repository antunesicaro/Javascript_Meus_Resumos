const nome = 'icaro';
const obj = {nome};
const novoObj = {...obj};
console.log(novoObj);

//obs: //toda vez que salva o functions.js, recarrega o bludle automático lá nop babelPorIcaro no pacackges.json
//comando no terminal pra automatizar esse processo: npm run babel